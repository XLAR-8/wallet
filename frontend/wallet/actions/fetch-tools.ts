"use server";


export const fetchTools = async (page: number) => {
    // const limit = PAGE_RESULT_LIMIT;
    // const tools = await db.products.findMany({
    //     skip: (page - 1) * limit,
    //     take: limit,
    //     orderBy: {
    //         id: "asc"
    //     }
    // });

    return "tools";
}